import styled from "styled-components"

const Navbar = styled.div`
  display: flex;
  justify-content: space-between;
  margin-left: auto;
  margin-right: auto;
  left: 0;
  right: 0;
  width: 85vw;
`

export { Navbar }
