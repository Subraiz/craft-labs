import styled from "styled-components"

const Address = styled.p`
  z-index: 2;
  line-height: 0;
  font-family: "Ubuntu", sans-serif;
  color: white;
  font-size: 18px;
  font-weight: 500;
`

export { Address }
